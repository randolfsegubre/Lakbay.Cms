## Copy to .env (gitignored) and fill in a real value before running
## `docker compose up`. This is a local-dev-only SQL Server SA password —
## never reused for any real/deployed environment.
DB_PASSWORD=ChangeMe_Local_Dev_Only_1!
